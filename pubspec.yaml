name: tokenizer_parser
description: A lightweight tokenizer and parser-composition toolkit for Dart, designed to build custom language grammars and structured token streams.
version: 0.1.3
repository: https://github.com/roadroot/tokenizer

environment:
  sdk: '>=3.5.0 <4.0.0'

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
